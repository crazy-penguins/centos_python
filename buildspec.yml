---
version: 0.2
phases:
  install:
    commands:
      - "python -m platform"
      - "aws --version"
      - "cat /etc/docker.conf"
  build:
    commands:
      - "export version=3.6.7"
      - |
        cd "/work" \
          && ./centos_python.sh "${version}" \
          && cd /u/python-${version} \
          && zip -q -r -m ${CODEBUILD_SRC_DIR}/python-${version}.zip .
    # pre_build:
  # post_build:
artifacts:
  files:
    - "python-*.zip"
#    - nginx-1.14.2.zip
#    - "maxmind-*.zip"
